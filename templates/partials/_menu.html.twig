<header class="bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
        <a href="{{ path('home') }}" class="text-2xl font-bold text-teal-600">
            Vérité Voyages
        </a>

        <nav class="space-x-6">
            <a href="{{ path('home') }}" class="hover:text-teal-600">Home</a>
            <a href="{{ path('destinations') }}" class="hover:text-teal-600">Destinations</a>
            <a href="{{ path('tours') }}" class="hover:text-teal-600">Tours</a>
            <a href="{{ path('contact') }}" class="hover:text-teal-600">Contact</a>
        </nav>
    </div>
</header>